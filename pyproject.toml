[project]
name = "vectorless-rag"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pydantic-ai>=2.32.0",
]


[project.scripts]
vectorless = "vectorless_rag.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
